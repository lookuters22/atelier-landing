# Intended live V3 gate posture for harness **preflight** (mirror Supabase Edge secrets).
# Copy to `scripts/v3_verify_gate_posture.env` (gitignored) or set `V3_VERIFY_GATE_POSTURE_FILE`.
# Only the keys below are applied (override base `.env` for these variables).

TRIAGE_LIVE_ORCHESTRATOR_MAIN_PATH_CONCIERGE_KNOWN_WEDDING_V1=1
TRIAGE_LIVE_ORCHESTRATOR_MAIN_PATH_PROJECT_MANAGEMENT_KNOWN_WEDDING_V1=1
TRIAGE_LIVE_ORCHESTRATOR_MAIN_PATH_LOGISTICS_KNOWN_WEDDING_V1=1
TRIAGE_LIVE_ORCHESTRATOR_MAIN_PATH_COMMERCIAL_KNOWN_WEDDING_V1=1
TRIAGE_LIVE_ORCHESTRATOR_MAIN_PATH_STUDIO_KNOWN_WEDDING_V1=1

# Optional: require live orchestrator after intake bootstrap (intake.ts, email only)
# INTAKE_LIVE_ORCHESTRATOR_POST_BOOTSTRAP_EMAIL_V1=1

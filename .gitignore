# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Cursor IDE (local plans / state — not required for deploy)
.cursor/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local scratch / old scaffolds
_tmp/
_wd.py
_dl.py

# Reference / research files (not part of the app)
HEROEXTRACT/
chunks/
csschunks/
meetcleo*
parallax.psd
HARDCODED_DATA_AUDIT.md
persona agent examples context/
public/old_background.jpg

# Original JPGs (replaced by .webp)
public/landing/*.jpg

# Unused 3D models
public/models/custom_iphone_17_3d_model.glb

# Reference PDFs (add to repo explicitly if needed)
*.pdf

# Environment Variables (Keep Secrets Safe)
.env
.env.*
!.env.example
supabase/.env

# V3 verification harness — local mirror of Edge triage/intake gate secrets (copy from scripts/v3_verify_gate_posture.env.example)
scripts/v3_verify_gate_posture.env

supabase/functions/inngest/.qa_fixtures.json

# Playwright
/test-results/
/playwright-report/
/blob-report/

# Local QA harness output (timestamped JSON/MD)
reports/

# Supabase CLI local cache (machine-specific)
supabase/.temp/
